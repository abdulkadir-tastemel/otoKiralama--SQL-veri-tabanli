@model IEnumerable<RentCars.Models.carsTable>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Yeni Ekle", "Create")
    </p>
    <table class="table" border"1" cellpadding="1"cellspacing="0">
        <tr>
            <th>.</th>
            <th>
                <p>ARABA ADI</p>

            </th>
            <th>
                <p>MODEL</p>
            </th>
            <th>
                <p>TARİH</p>
            </th>
            <th>
                <p>VİTES</p>
            </th>
            <th>
                <p>BENZİN</p>
            </th>
            <th>
                <p>FİYAT $</p>
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
    <tr>
        <td>
            @{
                var dir = "/VehicleImages/" + item.Id + ".jpg";
                <img src="@Url.Content(dir)" width="100" />
            }
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CarsName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CarsModel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.carsDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.carsGear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.carsGas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.carsPrice)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Detaylar", "Details", new { id = item.Id }) |
            @Html.ActionLink("Sil", "Delete", new { id = item.Id })
            @Html.ActionLink("Resim Yükle", "SavageImages", new { id = item.Id })
        </td>
    </tr>
    }
    
    </table>
</body>
</html>
